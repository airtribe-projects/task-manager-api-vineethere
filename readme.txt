Hii, 
My Actual APIs redside in the location of taskManager\routes\tasks.js
logics are written here , 
Following APIs are listed as we run our project , 

GET /tasks: Retrieve all tasks.
GET /tasks/:id: Retrieve a specific task by its ID.
POST /tasks: Create a new task with the required fields (title, description, completed).
PUT /tasks/:id: Update an existing task by its ID.
DELETE /tasks/:id: Delete a task by its ID.

All the API inputs are validated if any wrong request is coming , 
As of now due to office worekload , I have skipped the optional Extensions , I will definitely upload it after completion , I will complete this by my side also 

For starting just use the below command (without ')

'npm start'